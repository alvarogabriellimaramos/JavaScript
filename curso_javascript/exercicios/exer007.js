const produto = {
    nome: 'Notebook',
    preço: 2500,
    quantidade: 3 
}
console.log(`O produto ${produto.nome} está com o preço ${produto.preço} e no estoque temos a quantidade de ${produto.quantidade} ${produto.nome}`)