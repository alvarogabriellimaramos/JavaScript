// condicionais são uma forma de deixa o nosso código um pouco mais inteligente,elas server para da opturnidade de decisão nosso código,por exemplo,se a váriavel numero for 1 ela imprimir 'váriavel'
let numero = 2
if (numero === 1) console.log('Váriavel um') // utilizamos dois ou três igual para fazer a comparação,um igual é comparação normal,ou seja irá compara apenas o valor,já os três iguais irá fazer uma comparação de tipo de dados,por recomendação iremos utiliza o igual ===

// temos o else que é um caso opcional,mas também é muito utilizado,a regra do else funciona da seguinte forma,'se (if) a váriavel um for igual a 1 imprima tal coisa', 'senão (else) faça tal coisa'
else{
    console.log('Variavel não é igual a 1')
}